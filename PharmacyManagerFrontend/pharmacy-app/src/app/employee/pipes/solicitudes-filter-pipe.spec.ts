import { SolicitudesFilterPipe } from "./solicitudes-filter.pipe";

describe('SolicitudesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SolicitudesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
